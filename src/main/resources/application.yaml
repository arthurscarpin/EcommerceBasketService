spring:
  application:
    name: basketservice

  data:
    mongodb:
      host: localhost
      port: 27017
      database: basket-service
    redis:
      host: localhost
      port: 6379
      password: sa

  cache:
    redis:
      time-to-live: 600000

basket:
  client:
    platzi: https://api.escuelajs.co/api/v1

security:
  jwt:
    secret: my-secret-key